

import "bootstrap";
// import { toggleActive } from 'components/dashboard';
import { initAutocomplete } from 'plugins/init_autocomplete';

$('.carousel').carousel({
  touch: true,
  interval: false,
})

// toggleActive();
initAutocomplete()
